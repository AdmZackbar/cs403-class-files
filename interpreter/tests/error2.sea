class Error2
{
    function tester()
    {
        a[2] = d;
        x < 4;
        // incorrect syntax - while loops need a block -> while ( (expr) ) { (statements) }
        while (x == 2)
            doSomething();
    }
}
